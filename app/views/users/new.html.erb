<div class="row">
  <div class="col-xs-12 col-sm-8  col-sm-offset-2">
    <h2>Create your profile: </h2>
    <%= form_for @user do |f| %>
      <div class="form-group">
        <%= f.label :first_name %>:
        <%= f.text_field :first_name, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :last_name %>:
        <%= f.text_field :last_name , class: "form-control"%>
      </div>
      <div class="form-group">
        <%= f.label :email %>:
        <%= f.email_field :email , class: "form-control"%>
      </div>
      <div class="form-group">
        <%= f.label :password %>:
        <%= f.password_field :password , class: "form-control"%>
      </div>

      <% if flash[:password_error] %>
        <p class="flash-error">Passwords must match.</p>
      <% end %>

      <div class="form-group">
        <%= f.label :password_confirmation %>:
        <%= f.password_field :password_confirmation, class: "form-control"%>
      </div>
      <div class="form-group">
        <%= f.label :image_url %>:
        <%= f.url_field :image_url, placeholder: "Image URL" , class: "form-control"%>
      </div>
      <div class="form-group">
       <%= f.submit "Save User", class: "form-control btn btn-primary" %>
      </div>
    <% end %>
    <a href="/login">Already a member? Log in &raquo;</a>
    </div>
 </div>
