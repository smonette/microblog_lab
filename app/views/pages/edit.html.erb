
<div class="container">
  <h1>Edit Your Profile</h1>

  <%= form_for ([@user, @page]) do |f| %>
    <%= f.label :name %>:<br>
    <%= f.text_field :name, placeholder: "About or Contact" %><br>

    <%= f.label :content %>:<br>
    <%= f.text_area :content %><br>
    <%= f.submit %>
  <% end %>
</div>
